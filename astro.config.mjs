import { defineConfig } from 'astro/config';

// Deployed to GitHub Pages at https://<username>.github.io/behavioral-questions
// If you fork this, update `site` to your own username.
export default defineConfig({
  site: 'https://jackdo68.github.io',
  base: '/behavioral-questions',
});
